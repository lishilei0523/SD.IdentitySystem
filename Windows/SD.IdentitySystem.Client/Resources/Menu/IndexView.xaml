<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:customControls="clr-namespace:SD.Infrastructure.WPF.CustomControls;assembly=SD.Infrastructure.WPF"
                    xmlns:cal="http://www.caliburnproject.org">

    <!--菜单TreeListView列定义-->
    <GridViewColumnCollection x:Key="Menu.TreeListView.ColumnDefinitions">
        <!--行号列-->
        <GridViewColumn Width="35" CellTemplate="{StaticResource TreeListView.CellTemplates.RowIndex}" />
        <GridViewColumn Header="名称" Width="250" CellTemplate="{StaticResource TreeListView.CellTemplates.Criterion}" />
        <GridViewColumn Header="链接地址" Width="400">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border}">
                        <TextBlock Text="{Binding Url}" Style="{StaticResource TextBlock.Styles.Inline}" />
                    </Border>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
        <GridViewColumn Header="路径" Width="200">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border}">
                        <TextBlock Text="{Binding Path}" Style="{StaticResource TextBlock.Styles.Inline}" />
                    </Border>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
        <GridViewColumn Header="所属系统" Width="210">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border}">
                        <TextBlock Text="{Binding SystemName}" Style="{StaticResource TextBlock.Styles.Inline}" />
                    </Border>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
        <GridViewColumn Header="应用程序类型" Width="150">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border}">
                        <TextBlock Text="{Binding ApplicationType, Converter={StaticResource EnumMemberConverter}}"
                                   Style="{StaticResource TextBlock.Styles.Inline}" />
                    </Border>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
        <GridViewColumn Header="创建时间" Width="170">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border}">
                        <TextBlock Text="{Binding AddedTime, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}"
                                   Style="{StaticResource TextBlock.Styles.DateTime}" />
                    </Border>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
        <GridViewColumn Header="关联权限" Width="75">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border}">
                        <TextBlock x:Name="RelateAuthorities" Text="关联权限" 
                                   cal:Message.Attach="[Event MouseLeftButtonDown] = [Action RelateAuthorities($dataContext)]"
                                   Style="{StaticResource TextBlock.Styles.Hyperlink}" />
                    </Border>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=HasItems, RelativeSource={RelativeSource AncestorType={x:Type customControls:TreeListViewItem}}}" 
                                     Value="True">
                            <Setter TargetName="RelateAuthorities" Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
        <GridViewColumn Header="编辑" Width="45">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border}">
                        <TextBlock Text="编辑" 
                                   cal:Message.Attach="[Event MouseLeftButtonDown] = [Action UpdateMenu($dataContext)]"
                                   Style="{StaticResource TextBlock.Styles.Hyperlink}" />
                    </Border>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
        <GridViewColumn Header="删除" Width="45">
            <GridViewColumn.CellTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource TreeListView.Styles.Border.End}">
                        <TextBlock Text="删除"  
                                   cal:Message.Attach="[Event MouseLeftButtonDown] = [Action RemoveMenu($dataContext)]"
                                   Style="{StaticResource TextBlock.Styles.Hyperlink}" />
                    </Border>
                </DataTemplate>
            </GridViewColumn.CellTemplate>
        </GridViewColumn>
    </GridViewColumnCollection>
    <!--菜单TreeListView控件模板-->
    <ControlTemplate x:Key="Menu.TreeListView.ControlTemplate" TargetType="{x:Type customControls:TreeListView}">
        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <DockPanel>
                    <GridViewHeaderRowPresenter DockPanel.Dock="Top" 
                                                Columns="{StaticResource Menu.TreeListView.ColumnDefinitions}" 
                                                Height="27" >
                        <GridViewHeaderRowPresenter.ColumnHeaderContainerStyle>
                            <Style />
                        </GridViewHeaderRowPresenter.ColumnHeaderContainerStyle>
                    </GridViewHeaderRowPresenter>
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ItemsPresenter />
                    </ScrollViewer>
                </DockPanel>
            </ScrollViewer>
        </Border>
    </ControlTemplate>
    <!--菜单TreeListViewItem控件模板-->
    <ControlTemplate x:Key="Menu.TreeListViewItem.ControlTemplate" TargetType="{x:Type customControls:TreeListViewItem}">
        <StackPanel>
            <Border x:Name="PART_Border" 
                    Background="{TemplateBinding Background}" 
                    BorderBrush="{TemplateBinding BorderBrush}" 
                    BorderThickness="{TemplateBinding BorderThickness}" 
                    Padding="{TemplateBinding Padding}">
                <GridViewRowPresenter x:Name="PART_Header" 
                                      Height="23"
                                      Content="{TemplateBinding Header}" 
                                      Columns="{StaticResource Menu.TreeListView.ColumnDefinitions}" />
            </Border>
            <ItemsPresenter x:Name="ItemsHost" />
        </StackPanel>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
            </Trigger>
            <Trigger Property="IsExpanded" Value="False">
                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="PART_Border" Property="Background" Value="#FFE48D" />
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True" />
                    <Condition Property="IsSelected" Value="False" />
                    <Condition Property="HasItems" Value="False" />
                </MultiTrigger.Conditions>
                <Setter TargetName="PART_Border" Property="Background" Value="#EAF2FF" />
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <!--菜单TreeListView样式-->
    <Style x:Key="Menu.TreeListView.Styles" TargetType="{x:Type customControls:TreeListView}"
           BasedOn="{StaticResource TreeListView.Styles.Default}">
        <Setter Property="Template" Value="{StaticResource Menu.TreeListView.ControlTemplate}" />
    </Style>
    <!--菜单TreeListViewItem样式-->
    <Style x:Key="Menu.TreeListViewItem.Styles" TargetType="{x:Type customControls:TreeListViewItem}"
           BasedOn="{StaticResource TreeListViewItem.Styles.Default}">
        <Setter Property="Template" Value="{StaticResource Menu.TreeListViewItem.ControlTemplate}" />
    </Style>
</ResourceDictionary>
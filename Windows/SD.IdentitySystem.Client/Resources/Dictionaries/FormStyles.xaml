<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:kino="https://github.com/DinoChan/Kino.Toolkit.Wpf">

    <!--表单元素默认样式-->
    <Style TargetType="{x:Type kino:FormItem}">
        <Setter Property="Padding" Value="3" />
    </Style>
    <!--功能面板边框样式-->
    <Style x:Key="FunctionPanelBorder" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="#CCCCCC" />
        <Setter Property="BorderThickness" Value="0,1,0,1" />
    </Style>
    <!--表单提交Grid样式-->
    <Style x:Key="FormSubmitGrid" TargetType="{x:Type Grid}">
        <Setter Property="Background" Value="{StaticResource SystemListLowColorBrush}" />
        <Setter Property="Margin" Value="0,0,0,10" />
    </Style>
    <!--表单提交WrapPanel样式-->
    <Style x:Key="FormSubmitWrapPanel" TargetType="{x:Type WrapPanel}">
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,10,0" />
    </Style>
</ResourceDictionary>